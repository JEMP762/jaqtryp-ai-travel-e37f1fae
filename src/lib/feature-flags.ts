/**
 * Interruptores de funcionalidades da interface.
 * Nada é removido do sistema — apenas ocultado.
 */

/**
 * Assinaturas recorrentes (Pro / Ultra).
 * false = ocultas na interface (rota /billing continua funcionando).
 * Troque para true para reexibir planos e o item "Minha Assinatura".
 */
export const SUBSCRIPTIONS_ENABLED = false;

/**
 * Dashboard de Hospedagem no menu.
 * false = oculto (rota /stays continua existindo).
 */
export const STAYS_ENABLED = false;
